# Recovered-course and ASK release — September 8, 2026

The 86 recovered courses now have complete course and lesson pages, all 780 lessons and 5,334 original blocks, original feedback and work-product fields, and 44 recovered introduction recordings. Original release packs and available base-authored packs remain preserved in the immutable donor snapshot. Program-authored objective additions are recorded separately in the next-pass evidence directory.

Learning retains its existing tile format. Scoped owner publications determine which courses and lessons staff and ASK can see. Every recovered internal reference has a working current destination; retired course and community paths redirect to their current equivalents. Owner controls remain authenticated. Notes and learning progress stay in the current browser.

ASK's meeting fallback now supplies relevant practical steps. Published course and lesson content is indexed alongside the existing program resources. Retrieval avoids duplicating whole course bodies and reuses validated bundled publications. Research requests use exact source URLs, and ambiguous provider citation IDs are not silently assigned to the wrong page.

Verification completed locally: 1,207 tests passed across 123 files; the production build and TypeScript checks passed. Every lesson's interactive controls were exercised in React DOM, with additional real-browser reload, navigation, and scope checks. All 1,082 course-page/media requests and all 71 recovered internal source links passed. All 44 MP3 files decoded completely. Exact source parity and real database save, stale-write rejection, scoped withdrawal, inheritance, and restoration passed. A 100-concurrent-ASK synthetic-provider run completed without lost answers or task receipts. These bounded checks are not a 4,000-user capacity certification.

A real ASK test after course integration encountered an intermittent generation failure. Its fallback was relevant and its exact limited response was recorded; a subsequent real request returned a tailored answer and saved its complete record. The current-WCAG research answer passed with exact durable recording. Preserve both successful and limited-response receipts; do not describe provider availability as guaranteed.

The fresh encrypted production backup restored successfully into an isolated PostgreSQL 17 database with all 46 data tables. Migrations 0036–0038 applied additively. Two application-client publication attempts lost their connection and rolled back. The native PostgreSQL import then committed all 86 courses; all 172 exact scoped checks passed. Replays preserve later owner edits, withdrawals, and theme removals.

The separate preview project now has preview-scoped configuration and a confirmed READY deployment. It uses static approved content and temporary memory, without production database credentials; it is not a durable-production-data replica.

Release evidence: `evidence/next-pass-2026-09-08/`. Vercel deployment and post-release receipts are added after the GitHub-triggered build is verified.
