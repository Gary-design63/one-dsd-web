# Corpus and editing continuation

The owner directed: “This shall happen, proceed.” The work continues on `pac/corpus-editing-2026-09-05`, based on the release-tested `f3af662`, in the existing clean Downloads working folder. The reconstruction and both OneDrive development histories remain preserved.

## Verified changes

The 60 staged source objects, totaling 46,613,473 bytes, are now in the private `one-dsd-private` bucket of Supabase project `qhiawdhehhfuccxvhldo`. Each upload was verified by downloading it and matching the original fingerprint and size. A full replay found all 60 unchanged. There are 60 durable placement records and 24 explicit holds, including the known unavailable originals and required media work. Source preservation does not publish source files.

The first graph import created 1,068 nodes, 3,672 relationships, and 2,888 evidence links. It preserves structural connections from recorded source decisions and revision links; it does not infer staff characteristics or invent editorial approval. A second import created no duplicates. The graph import uses batches and excludes material explicitly classified as prohibited. Candidate source relationships remain private. Related-reading queries must join the current published revision so withdrawal takes effect immediately.

The resource editor now carries the selected One DHS or One DSD scope from the authenticated owner's request through the draft and review calls. A misplaced pair of review arguments was corrected. Recovered, unpublished resource candidates can be opened for editing in the collection workspace after the new migration is applied; saving creates a draft and does not publish it.

## Count reconciliation

Earlier summaries reported 52 courses and 56 community briefs. The pinned executable registries require correction: the later donor contains 86 course packs (45 authored packs and 41 generated cultural-intelligence packs), 40 indexed community briefs, eight religion briefs, and one additional guide. These distinctions are preserved in the donor import. No missing records are invented to reproduce earlier reported totals.

The 25 permanent seed resources remain an append-only collection. Donor courses, community material, and the 34 reconstruction domain resources are additional candidates. Their complete original structured content is preserved separately from any staff-readable adaptation.

## Remaining release work

Universal page editing, full donor import, editorial review, trusted publication, scoped graph retrieval, and browser verification remain in progress. Production at `https://one-dhs-pac.vercel.app/` has not been replaced by this branch. New SQL migrations are validated in a fresh local database before applying them to staging. Existing applied migration files are not rewritten.

No source cleanup, credential publication, agency-system integration, or DHS-logo change is part of this continuation.
