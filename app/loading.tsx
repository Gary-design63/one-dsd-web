export default function Loading() {
  return <div className="wrap py-12" role="status" aria-live="polite"><p className="text-lg font-semibold text-[#003865]">Opening your page…</p><p className="mt-2 text-muted">You can still use the navigation while it loads.</p></div>;
}
